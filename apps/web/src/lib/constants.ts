export const API_BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8000";

export const decisionSupportCopy =
  "Decision-support only. LendSignal 360 does not issue final credit approval; all outputs require human review, policy checks, and verified documents.";
